<Window x:Class="PS2IsoManager.Views.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing..."
        Width="420" Height="180"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Border Background="{DynamicResource BackgroundBrush}" CornerRadius="8"
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.6" BlurRadius="16" ShadowDepth="2"/>
        </Border.Effect>

        <Grid Margin="24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="MessageText" Grid.Row="0" Text="Splitting ISO..."
                       FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}"
                       Margin="0,0,0,12"/>

            <ProgressBar x:Name="Progress" Grid.Row="1" Height="8"
                         Minimum="0" Maximum="100" Value="0"
                         Margin="0,0,0,8"/>

            <TextBlock x:Name="PercentText" Grid.Row="2" Text="0%"
                       FontSize="12" Foreground="{DynamicResource TextDimBrush}"
                       HorizontalAlignment="Right"/>

            <Button x:Name="CancelButton" Grid.Row="3" Content="Cancel"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Padding="16,6" Click="CancelButton_Click"
                    Background="{DynamicResource DangerBrush}"/>
        </Grid>
    </Border>
</Window>
